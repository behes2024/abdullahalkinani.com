# Quarto
/.quarto/
/_site/
*_files/
*.html
!includes/*.html

# R
.Rproj.user
.Rhistory
.RData
.Renviron

# Python
.venv/
__pycache__/

# OS
.DS_Store
Thumbs.db

**/*.quarto_ipynb

# Raw/unpublished research data and internal documents — never publish these
# on the public site or in the public repo (site coordinates, individual
# specimen records, trial-level results, internal proposal draft).
assets/*.xlsx
assets/*.xls
assets/*.csv
assets/Abdullah proposal RLF4.docx

# Temporary local source for photo review — full-resolution originals with
# EXIF/GPS intact. Never commit this folder; only vetted, stripped copies
# selected from it go into assets/images/.
.image-source/
